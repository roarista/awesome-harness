NOW: Codex at real parity — ~/.codex/ has code-decompose, compact-prep (MINIMUM PATH), check-all, recall, BUILDER/MEMORY_STANDARD and its own tools/check-all/; AGENTS.md (76->102 lines) is a router table pointing at each by path. ADAPTED not copied: no subagent fleet, so Codex decomposes then runs units sequentially, self-auditing each vs spec.
LAST_VERIFIED: 2026-07-27 — ./install.sh --codex deployed it all; bash -n install.sh + ~/.codex/tools/check-all/check_all.sh pass; memgraph mem.py query + ml reachable from Codex.
NEXT: (a) Stop-hook caveman counter = Ro's call; (b) understand-gate still warn; (c) map auto-refresh unwired; (d) ~/awesome-harness clone UU .now.md; (e) northstar-protect mention-match inversion; (f) ~/.codex/skills/codex-primary-runtime/ EMPTY dir.
