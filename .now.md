NOW: SHIPPED c39fd91 (116). Edit is the builder rule; main writes no files (haiku does); spawn prompts capped ~200w; codemap runs in EVERY repo now and can never exceed 24KB; .findings folders rotate by date.
LAST_VERIFIED: 2026-08-03 — intrn codemap 347,964 -> 24,000 bytes with #TRUNCATED; codemap-inject --selftest 3 PASS exit 0; finding.sh rotated live into a 08-03 folder, 08-02 untouched; check-all READY; origin==HEAD.
NEXT: MCP cut is Ro's UI action (~165 of ~280 tools are claude.ai connectors, not local config; local = fred/databento/sparktoro/obsidian). Then repowise index stale (do LAST).
