NOW: SHIPPED+pushed 468067d. Trimmer RUNS weekly: launchd drives Terminal.app via osascript (borrows its TCC grant; no FDA needed). git-sync.sh stamps `Terminal: <id>` on every commit + read-only AHEAD/RECENT/STALE branch survey.
LAST_VERIFIED: 2026-08-01 — launchctl exit 0, err.log empty, 5 fresh reports today. Auditor ACCEPT-WITH-FIXES; all 4 fixes applied+reverified. Repo shim == installed copy.
NEXT: (1) decide ~/.route-only (verdict: DELETE); (2) stop-hook violation counter; (3) version ~/.claude/agents/*.md. Detail: .planning/STATE.md
