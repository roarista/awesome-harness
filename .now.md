NOW: SHIPPED 4feff48 (100). Semgrep sweep found 3 loose cables — `grep -r` is ugrep --ignore-files (false zeros), semgrep can't parse finding.sh/git-sync.sh, and our own 5 rulesets never ran in check-all. Fixed; .findings/ bootstrapped per-repo.
LAST_VERIFIED: 2026-08-03 — check-all semgrep row now reads "7 UNSCANNED (parse error); repo-rules: 84 (except-swallows 52, deny-site 13, +2)", OVERALL READY, no-op proven without tools/semgrep; origin==HEAD.
NEXT: triage the 84 repo-rule findings (52 except-swallows) · make finding.sh + git-sync.sh semgrep-parseable · repowise index stale · measure codex-audit vs opus.
