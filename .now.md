NOW: Inverted-guard defects fixed. main-edit-guard now allows `.scratch/**`, `.mulch/**`, `.now.md`, STATE (it was blocking the discovery artifact `codebase-first` requires AND every compaction-safe turn close). builder-fence's north-star check now matches write-shaped targets instead of any substring mention; its CONTEXT/CHANGE/GOAL/VERIFY/REUSE demand is now nudge-only (a Sonnet forwarding wrapper structurally cannot satisfy it, and `understand-gate` already enforces it at PreToolUse(Task) where the prompt is visible). The Bash write hole is DOCUMENTED, deliberately not half-closed. Per-turn harness-enforce injection cut 64 -> ~15 tokens (caveman segment 238 -> 66 chars).
LAST_VERIFIED: 2026-07-27 — real exit codes: north-star mention now 0, five genuine write shapes still 2, `.scratch`/`.now.md` writes now 0, `.py` write still 2, missing-brief now 0 with a nudge; both selftests pass. harness-enforce piped a UserPromptSubmit payload: exit 0, injected text confirmed; synced to ~/.claude/hooks (diff -q clean).
NEXT: (a) Ro's call on the Stop-hook caveman violation counter (detection only — blocking mid-turn prose is impossible, no hook event exists for assistant text); (b) understand-gate still `warn`; (c) map auto-refresh still unwired; (d) `~/awesome-harness` stale clone still has an unresolved `UU .now.md` conflict.
