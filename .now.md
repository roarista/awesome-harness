NOW: understand-gate.py shipped + registered (PreToolUse Task: code-writing subagent spawns must carry codebase-first reuse evidence; modes off/log/warn/enforce, default warn, fail-open); ctxproxy fully purged (.gitignore + awesomeharness SKILL.md dead-routes).
LAST_VERIFIED: 2026-07-27 — selftest PASS; UNDERSTAND_GATE=enforce blocks mutation-shaped spawn prompts (exit 2) and passes read-only prompts (exit 0); fail-open on malformed stdin; ~/.claude/settings.json parses with the PreToolUse `Task` matcher registered; bash -n install.sh OK.
NEXT: (a) run understand-gate in warn/log for a while, then consider flipping default to enforce; (b) port THE PROCEDURE to ~/.codex/AGENTS.md for Codex parity; (c) map auto-refresh (graphify watch + repowise reindex) still NOT wired.
