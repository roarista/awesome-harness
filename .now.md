NOW: Renamed /scale -> /awesomeharness (repo+global). Semgrep flipped to ADVISORY suggest-only (prints findings + "consider" reason, never blocks; SEMGREP_STRICT=1 to gate). Remote-control root cause LIKELY = NO_COLOR=1 + FORCE_COLOR=0 in settings.json env (matches Ro's "stripped color broke RC" memory); research agent confirming + I was WRONG that bg/agents can't be RC'd.
LAST_VERIFIED: 2026-07-27 — bash -n OK; /awesomeharness present repo+global, /scale removed; semgrep branch default warn.
NEXT: apply the settings.json env fix once research confirms (drop FORCE_COLOR=0 / NO_COLOR); then port /awesomeharness charter -> ~/.codex/AGENTS.md.
