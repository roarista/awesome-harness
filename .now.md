NOW: Semgrep ENFORCED by default (findings FAIL check-all; opt-out SEMGREP_STRICT=0). SkillOpt target GLM->Opus (config.yaml target=claude-opus-4-8[1m] via claude_chat). Gemini via opencode wired (needs Ro's OAuth).
LAST_VERIFIED: 2026-07-23 -- check_all.sh flip `bash -n` OK + regions grep-confirmed; SKILL.md docs synced; skillopt config.yaml swapped.
NEXT (Ro): (a) `opencode auth login` -> pick **Google** in the provider list (Gemini lives UNDER Google) -> then "OAuth with Google (Gemini CLI)"; confirm ids `opencode models | grep google`. (b) SkillOpt now Opus optimizer+target -- give cost OK to run. (c) noisy repo? `SEMGREP_STRICT=0`.
