NOW: SHIPPED b211e84 (96). tools/codemap.py + hooks/codemap-inject.py — whole repo (89f/11,537L) compressed to 7,475B (~1.9k tok), injected at SessionStart, regenerated when its @sha != HEAD.
LAST_VERIFIED: 2026-08-03 — hook fires live (7,704B), CODEMAP_INJECT=off gives 0B, stale-sha regenerates; RETURN CONTRACT now in all 4 agent defs (was in ZERO); repo skills/ == live ~/.claude/skills/ exactly; check-all OVERALL READY; origin==HEAD.
NEXT: understand-gate.py still tells agents to run the deleted `codebase-first` skill (say `orient`) · repowise index stale (15d7a1f vs b211e84) · wire route-model.sh into a real spawn path · measure codex-audit vs opus on a known-bad diff.
