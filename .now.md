NOW: Wave-scoped final-summary rule is now ENFORCED, not just hoped for. harness-enforce.py detects `<task-notification>` turns and replaces the rotating segments with one WAVE line; post-agent-guard leads with the wave rule (366->315 chars).
LAST_VERIFIED: 2026-07-27 — selftest PASS in 0.089s (no longer falls through to stdin); wave payload -> WAVE line only; normal payload -> normal segments only; malformed -> exit 0 silent; rotation no longer bumps on notification turns. Deployed live, diff -q clean.
NEXT: Ro's 2 open Qs (may MAIN write STATE.md — my answer: yes, keep; Stop-hook counter + delete the standing reminder). Then launchd audit exit-1, understand-gate=warn. Detail: .planning/STATE.md
