# Compact Handoff

_Auto-generated by global PreCompact hook at 2026-08-01 18:08 UTC. Read first after compaction._

**Branch:** `main`
**HEAD:** `d8f85e2`

## Last 20 commits
- d8f85e2 harness: awesomeharness skill teaches per-unit commit+push and maps new tools
- 68bbf87 harness: commit-often + never-lose-another-terminal's-work git sync
- 086676a harness: auditor findings -> terminal, trimmer, gate opt-in, live elite ledger
- 9fec6ad harness: wave-scoped final summary — task-notification turns get one WAVE rule
- 1c6152c harness: auditor memory (coach + scout) + retire per-turn file-path chat listing
- 70e1713 harness: dedupe blindspot registration, fail-loud coach, graded-submission guard
- d89589a harness: Codex parity — port decompose/compact-prep/check-all/recall + standards, AGENTS.md router
- a083ecc harness: boot-heavy / turn-light injection + /awesomeharness re-asserts the full floor
- e639ff5 harness: un-invert the guards (mention-matching -> write-matching) + slim the per-turn injection
- 47d4880 harness: close wiring + executability gaps (audited), Codex parity via AGENTS.md
- 751cd4b harness: understand-gate hook (codebase-first evidence required before code spawns) + ctxproxy fully purged
- 8ed5dfe harness: /awesomeharness = THE PROCEDURE + canonical 6-system map; purge ctxproxy from installer+README
- 4ba8b1a harness: unify orient workflow — graphify + repowise used TOGETHER in codebase-first
- afee9bc harness: document /awesomeharness in README + remove ctxproxy + semgrep excludes for scraped data
- c7d11ba harness: rename /scale -> /awesomeharness + semgrep back to ADVISORY suggest-only
- 0503ffe harness: /scale boot skill (one-command full-harness activation) + semgrep enforce scoped to p/default --severity ERROR (no wedge, no telemetry)
- 7298d11 harness: Semgrep enforced by default (SEMGREP_STRICT=0 to soften) + SkillOpt target GLM->Opus
- e928424 chore: .now.md -> GLM retired + Gemini wired (cef88ce)
- cef88ce harness: retire GLM (out of credits) -> Opus 4.8 low-effort auditor + Gemini via opencode
- 56c0771 chore: .now.md -> items 1-5 shipped (459e314)

## Working tree at compact time
```
 M .planning/COMPACT_HANDOFF.md
```

## Resume protocol
1. Read `.planning/STATE.md` -> `## Active Resume Point` (if present)
3. Continue from the resume point

## Load-bearing context

_Bounded context copied into the compact hook output so the next terminal sees it even if the summary is aggressive._

### .planning/STATE.md -> ## Active Resume Point

**Last updated:** 2026-08-01
**Status:** Auditor REJECT redispatch (13 ordered fixes + 1 extra) APPLIED by a fresh builder, uncommitted. Trimmer collision resolved: `tools/claudemd-trim.py` KEPT, `tools/claudemd_trim_audit.py` + `templates/…plist` DELETED. understand-gate reverted to global `warn` with repo opt-in via a `.understand-gate` marker (this repo is marked). check-all stamp moved to the END of the run and now carries `ok`. Scaffold capture is ambient off a GREEN check_all.sh (SCAFFOLD_CATEGORY/APPROACH/ITERS/AUDITOR env).
**Next concrete step:** Human review of the working tree, then commit. Separately: grant Full Disk Access to `/usr/bin/python3` (step 0 of `tools/launchd/INSTALL-claudemd-trim.md`) BEFORE loading `tools/launchd/com.ro.engineering-harness-audit.plist` — the launchd denial was that binary itself. Job stays UNLOADED until then.
**Blocked on:** This workspace cannot write or load `~/Library/LaunchAgents`; nested `codex exec` is sandbox-blocked, so live model-stage verification remains for the installed job.
